# Documentação - Programação | O que encontra aqui em Programação:

## JavaScript:
- **Básico**: Fundamentos da linguagem, sintaxe e estrutura.
- **Tipo de Dados**: String, Number, Boolean, Object, Array, Null, Undefined.
- **Array**: Como criar e manipular arrays.
- **Comandos de Entrada e Saída**: `console.log()`, `prompt()`, `alert()`.
- **Template Script**: Uso de templates literais para manipulação de strings.
- **For**: Estrutura de repetição `for`, `for...of`, `forEach()`.
- **Sistema para 3000ti - Abrigos**: Exemplo de uso para sistemas específicos.

---

## PHP:
- **Básico**: Fundamentos de PHP, estrutura e sintaxe básica.
- **Tipo de Dados**: String, Integer, Float, Array, Boolean, Object.
- **Array**: Manipulação de arrays (associativos e indexados).
- **Comandos de Entrada e Saída**: `echo()`, `print()`, `fgets()`, `readline()`.
- **For**: Estrutura de repetição `for`, `foreach()`.

---

## C++ / C#:
- **Básico**: Introdução à sintaxe e estrutura básica das linguagens.
- **Tipo de Dados**: Tipos primitivos como int, char, float, double, etc.
- **Array**: Criação e manipulação de arrays.
- **Comandos de Entrada e Saída**: `cin`, `cout` (C++), `Console.WriteLine()`, `Console.ReadLine()` (C#).
- **Template Script**: Uso de templates em C++ (funções e classes genéricas).
- **For**: Estrutura de repetição `for`, `foreach` (C#).

---

## COBOL:
- **Básico**: Estrutura de sintaxe e declaração de variáveis.
- **Tipo de Dados**: String, Numeric, Decimal, Alphanumeric, etc.
- **Array**: Manipulação de tabelas e arrays.
- **Comandos de Entrada e Saída**: `DISPLAY`, `ACCEPT`.
- **Template Script**: Uso de estruturas de template em COBOL.
- **For**: Estrutura de repetição, embora COBOL utilize `PERFORM` para loops.

---

## TypeScript:
- **Básico**: Introdução à linguagem TypeScript, tipo estático e sintaxe.
- **Tipo de Dados**: `string`, `number`, `boolean`, `array`, `object`, `any`.
- **Array**: Declaração e manipulação de arrays com tipos específicos.
- **Comandos de Entrada e Saída**: `console.log()`, `prompt()`, `alert()`.
- **Template Script**: Uso de template literals e interpolação de strings.
- **For**: Estrutura de repetição `for`, `for...of`, `forEach()`.

---

## Dart:
- **Básico**: Estrutura de sintaxe e uso da linguagem Dart.
- **Tipo de Dados**: `int`, `double`, `String`, `bool`, `List`, `Map`.
- **Array**: Manipulação de `List` e outros tipos de coleção.
- **Comandos de Entrada e Saída**: `print()`, `stdin.readLineSync()`.
- **Template Script**: Uso de interpolação de strings e `StringBuffer`.
- **For**: Estrutura de repetição `for`, `for...in`, `forEach()`.

---

## HTML:
- **Tipos**: Tipos básicos de dados em HTML como `text`, `number`, `email`, etc. Usados em formulários.

---

## SQL:
- **Básico de Banco**: Consultas básicas como `SELECT`, `INSERT`, `UPDATE`, `DELETE`, criação de tabelas, junções, etc.

---

## Java:
- **Básico**: Estrutura básica da linguagem Java, tipos de dados e sintaxe.
- **Tipo de Dados**: `String`, `int`, `float`, `boolean`, `char`, `Array`, `Object`.
- **Array**: Declaração e manipulação de arrays.
- **Comandos de Entrada e Saída**: `System.out.println()`, `Scanner`.
- **Template Script**: Uso de templates e generics em Java.
- **For**: Estrutura de repetição `for`, `foreach()`.

---

## C:
- **Básico**: Sintaxe básica e estrutura de programação em C.
- **Tipo de Dados**: Tipos primitivos como `int`, `float`, `char`, `double`.
- **Array**: Criação e manipulação de arrays.
- **Comandos de Entrada e Saída**: `printf()`, `scanf()`.
- **Template Script**: Como utilizar funções genéricas com macros.
- **For**: Estrutura de repetição `for`, `while`.

---

